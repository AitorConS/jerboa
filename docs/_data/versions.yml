# Auto-generated by .github/workflows/docs.yml from VERSION.md and kernel/VERSION.
# These committed values are only a fallback for local `jekyll build`; the
# published site regenerates them from the source files on every docs deploy.
cli: 0.51.2
kernel: 0.2.0
