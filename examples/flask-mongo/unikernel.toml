# Build + run spec for the Flask web front-end (MongoDB edition).
#
#   jerboa build . --name flask-mongo --pkg-source ops --port 8080
#
# Talks to the MongoDB VM over the compose network; see stack.yaml.

[build]
lang = "python"

[run]
memory = "256M"
ports = ["8080:8080"]
